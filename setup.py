from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Will explore categorical encoding, discretization, missing value treatment, feature scaling, handle outliers, and feature transformation in depth',
    author='ronilpatil',
    license='MIT',
)
